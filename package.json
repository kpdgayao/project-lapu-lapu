{
  "name": "project-lapu-lapu",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered voice customer service system for Philippine pharmaceutical company",
  "scripts": {
    "dev": "npm run dev --prefix apps/api",
    "build": "npm run build --prefix apps/api",
    "start": "npm run start --prefix apps/api"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
